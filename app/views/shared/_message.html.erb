<% if @message_items.any? %>

    <div class="container-fluid">
      <div class="row">
        <div class="col-md-12">



          <div class="digg_pagination"> <%= will_paginate @message_items %> </div>
                <%= render partial: 'shared/message_item', collection: @message_items %>
          <div class="digg_pagination"> <%= will_paginate @message_items %> </div>

        </div>
      </div>
    </div>


<% end %>

