<% provide(:title, ApplicationHelper::HOME_LINK_NAME) %>

<div class="center hero-unit">
  <h1>oDistGen News</h1>
  <!--  <%= link_to ApplicationHelper::SIGN_UP_NOW, signup_path, class: "btn btn-large btn-primary" %> -->
</div>
<%= render 'shared/message' %>
<!--
<% if signed_in? %>
    <div class="row">
      <aside class="span4">
        <section>
          <%= render 'shared/user_info' %>
        </section>
        <section>
          <%= render 'shared/micropost_form' %>
        </section>
      </aside>
      <div class="span8">
        <h3>Micropost Feed</h3>
        <%= render 'shared/feed' %>
      </div>
    </div>
<% else %>
    <div class="center hero-unit">
        <h1>oDistGen News</h1>
      <!--  <%= link_to ApplicationHelper::SIGN_UP_NOW, signup_path, class: "btn btn-large btn-primary" %> -->
    </div>
    <%= render 'shared/message' %>
<% end %>

-->